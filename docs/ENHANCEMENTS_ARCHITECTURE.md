# 🏗️ Enhancements Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CLIENT / BROWSER                                 │
│                                                                           │
│  ┌──────────────────────┐         ┌──────────────────────┐              │
│  │  Web Dashboard       │         │  API Client          │              │
│  │  (/status)           │         │  (AJAX Requests)     │              │
│  └──────────┬───────────┘         └──────────┬───────────┘              │
└─────────────┼───────────────────────────────┼──────────────────────────┘
              │                               │
              └───────────────┬───────────────┘
                              │
              ┌───────────────▼───────────────┐
              │    HTTP/HTTPS REQUESTS        │
              └───────────────┬───────────────┘
                              │
┌─────────────────────────────┼─────────────────────────────────────────────┐
│                    EXPRESS SERVER (app.js)                                │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ MIDDLEWARE LAYER                                                 │   │
│  │                                                                  │   │
│  │  ┌─────────────┐ ┌──────────────┐ ┌────────────────┐           │   │
│  │  │Rate Limiter │ │Request Logger│ │Error Handler   │           │   │
│  │  │(Blocks 429) │ │(Logs req/res)│ │(Last in chain) │           │   │
│  │  └──────┬──────┘ └──────┬───────┘ └────────┬───────┘           │   │
│  └─────────┼──────────────┼──────────────────┼──────────────────┘   │
│            │              │                  │                       │
│  ┌─────────▼──────────────▼──────────────────▼──────────────────┐   │
│  │ CACHING LAYER                                                │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────┐     │   │
│  │  │ Response Cache                                     │     │   │
│  │  │ ├─ /api/status (30s TTL)                          │     │   │
│  │  │ ├─ /api/config (60s TTL)                          │     │   │
│  │  │ └─ Other endpoints (configurable)                 │     │   │
│  │  └────────────────────────────────────────────────────┘     │   │
│  └──────────────────────────────────────────────────────────────┘   │
│            │                                                       │
│  ┌─────────▼──────────────────────────────────────────────────┐   │
│  │ ROUTING LAYER                                              │   │
│  │                                                            │   │
│  │  ┌──────────────────────────────────────────────────────┐ │   │
│  │  │ /api/status                    ─► getCachedStatus()  │ │   │
│  │  │ /api/health                    ─► getHealth()        │ │   │
│  │  │ /api/health/detailed           ─► getDetailedHealth()│ │   │
│  │  │ /api/metrics/performance       ─► getPerfMetrics()  │ │   │
│  │  │ /api/metrics/analytics         ─► getAnalytics()    │ │   │
│  │  │ /api/webhooks                  ─► manageWebhooks()  │ │   │
│  │  │ /status                        ─► htmlDashboard()   │ │   │
│  │  │ /api/logs/:level               ─► getLogs()         │ │   │
│  │  └──────────────────────────────────────────────────────┘ │   │
│  └────┬───────────┬───────────┬───────────┬───────────┬─────┘   │
└───────┼───────────┼───────────┼───────────┼───────────┼──────────┘
        │           │           │           │           │
        │           │           │           │           │
        │           │           │           │           │
   ┌────▼──┐   ┌───▼───┐  ┌────▼──┐  ┌───▼───┐  ┌────▼──┐
   │Logger │   │Health │  │Perf   │  │Pool   │  │Webhook│
   │Module │   │Mon    │  │Mon    │  │Mon    │  │Srv    │
   └────┬──┘   └───┬───┘  └────┬──┘  └───┬───┘  └────┬──┘
        │          │           │         │           │
        │          │           │         │           │
┌───────▼──────────▼───────────▼─────────▼───────────▼──────────┐
│ MONITORING & ANALYTICS LAYER                                  │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Analytics Service                                    │    │
│  │ ├─ Active user tracking                             │    │
│  │ ├─ Request volume metrics                           │    │
│  │ ├─ Error categorization                            │    │
│  │ ├─ Session tracking                                │    │
│  │ └─ Health score calculation                        │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Performance Monitor                                  │    │
│  │ ├─ Endpoint response times                          │    │
│  │ ├─ Database query performance                       │    │
│  │ ├─ Slow query detection                            │    │
│  │ └─ System resource tracking                        │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Health Monitor                                       │    │
│  │ ├─ Database health checks                           │    │
│  │ ├─ Memory usage monitoring                          │    │
│  │ ├─ Alert generation                               │    │
│  │ └─ Status history tracking                         │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Connection Pool Monitor                             │    │
│  │ ├─ Active connection tracking                       │    │
│  │ ├─ Idle connection monitoring                       │    │
│  │ ├─ Pool exhaustion alerts                          │    │
│  │ └─ Error tracking                                  │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────┬───────────────────────────────────────────────────┘
           │
    ┌──────▼──────────────┬──────────────┬──────────────┐
    │                     │              │              │
 ┌──▼──┐            ┌────▼────┐    ┌────▼────┐    ┌───▼────┐
 │Logs │            │Database │    │Webhooks │    │Metrics │
 │     │            │         │    │Queue    │    │Store   │
 │ -INFO.log        │Neon     │    │         │    │        │
 │ -ERROR.log       │Database │    │ Events: │    │ Time   │
 │ -WARN.log        │         │    │-Status  │    │ Series │
 │ -DEBUG.log       │Health   │    │-Alerts  │    │        │
 └─────┘            │Checks   │    │-Errors  │    └────────┘
                    └────────┘    └─────────┘
                         │
                    ┌────▼────────┐
                    │ Monitoring  │
                    │ Services    │
                    │ (External)  │
                    │             │
                    │ Slack       │
                    │ PagerDuty   │
                    │ DataDog     │
                    │ New Relic   │
                    └─────────────┘
```

---

## Data Flow Diagram

```
USER REQUEST
    │
    ▼
HTTP Server
    │
    ├─────────────────────────────────────────┐
    │ RATE LIMITER                            │
    │ Check: IP + Window                      │
    │ Result: Allow or 429 (Too Many)         │
    │         Allow: Continue                 │
    │         Block: Return 429               │
    └─────────────────────────────────────────┘
    │
    ▼
REQUEST LOGGER
    │ Captures:
    │ - Method, Path, IP
    │ - User Agent
    │ - Timestamp
    │
    ▼
    ├─────────────────────────────────────────┐
    │ CACHE CHECK                             │
    │ Key: method + path                      │
    │ Check if cached and not expired         │
    │                                         │
    │ IF HIT: ─────────────────────┐         │
    │ Return cached response       │         │
    │ Skip database               │         │
    │                             │         │
    │ IF MISS: ─────────┐         │         │
    │ Continue to route │         │         │
    └─────────┬─────────┼─────────┼────────┘
              │         │         │
              ▼         │         ▼
        ROUTE HANDLER   │    SEND RESPONSE
        │               │    │
        ├─ DB Query    │    ├─ Cache it
        ├─ Compute    │    ├─ Add timing headers
        ├─ Format     │    ├─ Log response
        │             │    └─ Track metrics
        └─────────────┘
              │
              ▼
        ANALYTICS
        │
        ├─ Track endpoint performance
        ├─ Track database performance
        ├─ Update active user count
        ├─ Track error rates
        └─ Calculate health score
              │
              ▼
        HEALTH CHECKS
        │
        ├─ Check memory usage
        ├─ Check DB connection
        ├─ Check error rates
        └─ Generate alerts if needed
              │
              ▼
        WEBHOOK QUEUE
        │
        ├─ Enqueue if alert
        ├─ Process with retries
        └─ Send to external services
              │
              ▼
        LOGGING
        │
        └─ Write to log files
           (ERROR.log, INFO.log, etc)
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────┐
│                    STATUS ROUTES                         │
│                                                         │
│  /api/status      /api/health      /api/metrics/**     │
│       │                 │                  │            │
│       └─────────────────┴──────────────────┘            │
│                      │                                   │
└──────────────────────┼───────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┬──────────────┐
        │              │              │              │
    ┌───▼──┐      ┌────▼────┐   ┌───▼───┐     ┌───▼────┐
    │Logger│      │Health   │   │Perf   │     │Analytics│
    │      │      │Monitor  │   │Mon    │     │Service  │
    └──┬───┘      └────┬────┘   └───┬───┘     └───┬────┘
       │                │            │            │
       │                │            │            │
       └────────┬───────┴────────────┴────────────┘
                │
        ┌───────▼────────┐
        │  Data Store    │
        │                │
        │ Metrics (RAM)  │
        │ History (RAM)  │
        │ Logs (Disk)    │
        └────────────────┘
                │
        ┌───────▼────────────────────┐
        │  External Services         │
        │                            │
        │ ✓ Webhooks                │
        │ ✓ Monitoring Services     │
        │ ✓ Alert Services          │
        └────────────────────────────┘
```

---

## Request Processing Timeline

```
TIME    COMPONENT              ACTION
──────────────────────────────────────────────────────────────
  0ms   HTTP Server            Receive request
        
  1ms   Rate Limiter           Check IP rate limit
                               ├─ If exceeded → Return 429
                               └─ If OK → Continue
        
  2ms   Request Logger         Log request metadata
  
  3ms   Cache Middleware       Check cache
                               ├─ If hit → Return cached (SKIP TO 18ms)
                               └─ If miss → Continue
        
  5ms   Router                 Match route handler
  
  6ms   Route Handler          Execute endpoint logic
        └─ Call DB service
        └─ Process data
        
 12ms   Format Response        Serialize result
 
 14ms   Cache Middleware       Cache response (if 200 status)
 
 15ms   Analytics              Track metrics
        └─ Update performance
        └─ Track endpoint usage
 
 16ms   Health Check           Check system health
        └─ Monitor memory
        └─ Check for alerts
 
 17ms   Response Logger        Log response
 
 18ms   HTTP Server            Send response to client
 
 20ms   Webhook Queue          Process alerts (async)
 
 25ms   Logging                Write to log files


CACHE HIT PATH (faster):
  0ms   HTTP Server            Receive request
  1ms   Rate Limiter           Check → OK
  2ms   Request Logger         Log
  3ms   Cache Middleware       ✓ HIT → Return
  5ms   Response Logger        Log
  6ms   HTTP Server            Send response

Savings: ~12ms (5x faster) for cached endpoints!
```

---

## Monitoring Stack

```
┌─────────────────────────────────────────┐
│      EXTERNAL MONITORING SERVICES       │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ Slack / PagerDuty               │   │
│  │ (Alert notifications)           │   │
│  └────────────┬────────────────────┘   │
│               │                        │
│  ┌────────────▼────────────────────┐   │
│  │ DataDog / New Relic             │   │
│  │ (Metrics aggregation)           │   │
│  └────────────┬────────────────────┘   │
│               │                        │
│  ┌────────────▼────────────────────┐   │
│  │ Custom Dashboards               │   │
│  │ (Real-time visualization)       │   │
│  └─────────────────────────────────┘   │
└────────────────┬──────────────────────┘
                 │
        ┌────────▼────────┐
        │  WEBHOOKS       │
        │                 │
        │ Queue:          │
        │ [status_change] │
        │ [alert_fired]   │
        │ [error_logged]  │
        └────────┬────────┘
                 │
    ┌────────────┼────────────────┐
    │            │                │
┌───▼──┐    ┌───▼───┐    ┌───────▼──┐
│Health│    │Perf   │    │Analytics │
│Mon   │    │Mon    │    │Service   │
└───┬──┘    └───┬───┘    └───┬──────┘
    │           │            │
    └───────────┼────────────┘
                │
        ┌───────▼──────────┐
        │  STATUS ROUTES   │
        │                  │
        │ HTML Dashboard   │
        │ JSON Endpoints   │
        │ Metrics API      │
        └──────────────────┘
```

---

## Error Handling Flow

```
REQUEST → ERROR OCCURS
           │
           ▼
    Error Handler Middleware
           │
           ├─ Categorize Error
           │  ├─ VALIDATION (400)
           │  ├─ AUTHENTICATION (401)
           │  ├─ AUTHORIZATION (403)
           │  ├─ NOT_FOUND (404)
           │  ├─ DATABASE (503)
           │  └─ APPLICATION (500)
           │
           ├─ Log Error
           │  ├─ File: ERROR.log
           │  ├─ Structured JSON
           │  └─ Include stack trace
           │
           ├─ Track in Analytics
           │  ├─ Error category
           │  ├─ Endpoint
           │  ├─ User
           │  └─ Timestamp
           │
           ├─ Check for Alerts
           │  ├─ High error rate?
           │  ├─ Critical error?
           │  └─ Pattern detected?
           │
           ├─ Queue Webhook
           │  └─ If alert needed
           │
           └─ Send Response
              ├─ Status: 400-500
              ├─ Error type
              ├─ Message
              └─ Request ID (for tracking)
```

---

## Health Monitoring Cycle

```
STARTUP
  │
  ▼
Initialize Health Monitor
  │
  ├─ Register webhooks
  ├─ Set thresholds
  └─ Start checks
  │
  ▼
Every 30 Seconds (periodic):
  │
  ├─ Run Database Health Check
  │  ├─ Execute test query
  │  ├─ Measure response time
  │  └─ Update status
  │
  ├─ Check Memory Usage
  │  ├─ Get heap usage
  │  ├─ Compare vs threshold
  │  └─ Alert if over 85%
  │
  ├─ Check Connection Pool
  │  ├─ Count active connections
  │  ├─ Calculate usage %
  │  └─ Alert if over 95%
  │
  ├─ Calculate Error Rate
  │  ├─ Count recent errors
  │  ├─ Compare vs threshold
  │  └─ Alert if over 5%
  │
  ├─ Update Status History
  │  ├─ Record component status
  │  ├─ Timestamp
  │  └─ Store last 100 changes
  │
  ├─ Generate Overall Status
  │  ├─ HEALTHY: All good
  │  ├─ DEGRADED: Some warnings
  │  └─ UNHEALTHY: Critical issues
  │
  └─ Notify Webhooks (if changed)
     ├─ Queue webhook events
     ├─ Retry with backoff
     └─ Track delivery
```

---

## Cache Strategy

```
REQUEST COMES IN
   │
   ▼
Generate Cache Key
   │
   ├─ Method: GET/POST
   ├─ Path: /api/status
   └─ Query: ?filter=...
   │
   ▼
Check Cache
   │
   ├─ KEY EXISTS?
   │  ├─ YES ───┐
   │  └─ NO ────┤
   │           │
   │           ▼
   │        Check Expiration
   │           │
   │           ├─ EXPIRED?
   │           │  ├─ YES → Delete
   │           │  └─ NO → Return cached data + X-Cache: HIT
   │           │
   │           ▼
   │        Execute Route Handler
   │           │
   │           ├─ Query Database
   │           ├─ Process Data
   │           └─ Format Response
   │           │
   │           ▼
   │        Cache Response
   │           │
   │           ├─ Key: method+path
   │           ├─ Value: response data
   │           ├─ TTL: configurable
   │           └─ Return data + X-Cache: MISS
   │           │
   └───────────┘

DEFAULT TTL VALUES:
  /api/status        → 30 seconds
  /api/health        → 10 seconds
  /api/metrics/*     → 60 seconds
  Other              → 300 seconds (5 min)

X-Cache Header Values:
  HIT   = Response from cache
  MISS  = Response from computation
```

---

## Deployment Architecture

```
┌──────────────────────────────────┐
│     LOAD BALANCER (Optional)     │
│  ├─ Round robin                  │
│  └─ Health check: /api/health    │
└──────────────────┬───────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
┌───▼────┐    ┌───▼────┐    ┌───▼────┐
│Instance │    │Instance │    │Instance │
│    1    │    │    2    │    │    3    │
│ Port:   │    │ Port:   │    │ Port:   │
│  5000   │    │  5001   │    │  5002   │
│         │    │         │    │         │
│ Express │    │ Express │    │ Express │
│ App     │    │ App     │    │ App     │
└────┬────┘    └────┬────┘    └────┬────┘
     │             │              │
     └─────────────┼──────────────┘
                   │
            ┌──────▼──────┐
            │  DATABASE   │
            │  (Neon)     │
            │             │
            │  - Neon PG  │
            │  - Replicas │
            │  - Backups  │
            └─────────────┘
```

---

## Summary

This architecture provides:

✅ **Protection:** Rate limiting prevents abuse
✅ **Performance:** Caching reduces database load
✅ **Monitoring:** Real-time metrics and analytics
✅ **Reliability:** Health checks and alerts
✅ **Observability:** Comprehensive logging
✅ **Scalability:** Metrics for optimization
✅ **Integration:** Webhook support for external services

All components work together to create a robust, observable, and performant application.
